{% extends 'base.html.twig' %}

{% block body %}
    <div class="col-xs-12">
        <h1>Créer un utilisateur</h1>
    </div>

    <div class="col-xs-12">
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Créer" class="btn btn-plus" />
        {{ form_end(form) }}
    </div>

    <div class="col-xs-12">
        <a href="{{ path('user_index') }}">Retour à la liste</a>
    </div>
{% endblock %}
